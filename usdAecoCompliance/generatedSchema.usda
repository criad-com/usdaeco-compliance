#usda 1.0
(
    "WARNING: THIS FILE IS GENERATED BY usdGenSchema.  DO NOT EDIT."
    fallbackPrimTypes = {
        token[] AecoRequirement = ["Scope"]
        token[] AecoSpecification = ["Scope"]
    }
)

class AecoSpecification "AecoSpecification" (
    customData = {
        string userDocBrief = "Document applicability and its independently addressable clauses."
    }
)
{
    string[] aeco:spec:appliesToClassification = [] (
        customData = {
            string userDocBrief = "System-qualified classification selectors."
        }
    )
    rel aeco:spec:appliesToTypes (
        customData = {
            string userDocBrief = "Catalog class paths matched through the element's composed inherits arcs."
        }
    )
    uniform token aeco:spec:kind = "regulation" (
        allowedTokens = ["regulation", "specification", "datasheet", "standard"]
        customData = {
            string userDocBrief = "Document authority category, never an element kind."
        }
    )
    string aeco:spec:source = "" (
        customData = {
            string userDocBrief = "Document and clause citation; illustrative sources must say so."
        }
    )
    rel aeco:spec:sourceDocument (
        customData = {
            string userDocBrief = "Optional relationship to a document record."
        }
    )
    string aeco:spec:title = "" (
        customData = {
            string userDocBrief = "Human title of this specification."
        }
    )
}

class AecoRequirement "AecoRequirement" (
    customData = {
        string userDocBrief = "A measured quantity or property comparison, with unit, tolerance and severity."
    }
)
{
    string aeco:req:measure = "" (
        customData = {
            string userDocBrief = "An element property or a measured quantity token; measured tokens use the derived body and spatial spine."
        }
    )
    uniform token aeco:req:operator = "between" (
        allowedTokens = ["eq", "ne", "lt", "le", "gt", "ge", "between", "in"]
        customData = {
            string userDocBrief = "Comparison operator."
        }
    )
    string aeco:req:rationale = "" (
        customData = {
            string userDocBrief = "Reason for the clause, including illustrative status where applicable."
        }
    )
    uniform token aeco:req:severity = "error" (
        allowedTokens = ["error", "warn"]
        customData = {
            string userDocBrief = "Error for a hard requirement; warn for an advisory requirement."
        }
    )
    string[] aeco:req:tokens = [] (
        customData = {
            string userDocBrief = "String benchmarks for eq, ne or in; mutually exclusive with values."
        }
    )
    double aeco:req:tolerance = 0 (
        customData = {
            string userDocBrief = "Finite nonnegative comparison tolerance in the declared unit."
        }
    )
    string aeco:req:unit = "m" (
        customData = {
            string userDocBrief = "SI unit: m for length, m2 for area, 1 for dimensionless values and tokens."
        }
    )
    double[] aeco:req:values = [] (
        customData = {
            string userDocBrief = "Finite numeric benchmarks in the declared unit; one for comparisons, two for between, one or more for in."
        }
    )
}

class "AecoComplianceAPI" (
    customData = {
        string userDocBrief = "Derived checked specifications, aggregate status and per-clause report on an element."
    }
)
{
    string aeco:compliance:report = "" (
        aecoDerived = true
        customData = {
            string userDocBrief = "One line per clause: citation, measure, measured value, unit and verdict."
        }
    )
    rel aeco:compliance:specifications (
        aecoDerived = true
        customData = {
            string userDocBrief = "The specifications actually checked, authored by the evaluator in its result layer."
        }
    )
    uniform token aeco:compliance:status = "unchecked" (
        aecoDerived = true
        allowedTokens = ["unchecked", "pass", "fail", "warn", "notApplicable"]
        customData = {
            string userDocBrief = "Aggregate result."
        }
    )
}

